# === apps/web (Next.js, Vercel) ===
NEXTAUTH_URL=http://localhost:3000
AUTH_SECRET=
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=
NEXT_PUBLIC_REALTIME_URL=ws://localhost:8080

# === apps/realtime (Fastify + Socket.IO, Railway) ===
REALTIME_PORT=8080
REALTIME_CORS_ORIGIN=http://localhost:3000
REALTIME_LOG_LEVEL=info

# === Banco (Postgres) ===
# Local dev: docker-compose sobe postgres:16-alpine em localhost:5432.
# Sprint 2+ (produção/preview): Neon — substitui esta URL pelo connection string da branch.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/soms_dev

# === Redis (Upstash) ===
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# === Providers de música ===
DEEZER_APP_ID=
DEEZER_APP_SECRET=
